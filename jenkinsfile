pipeline {

    agent any

    stages {

        stage('Check Node.js') {
            steps {
                bat 'node --version'
            }
        }

        stage('Run Registration Tests') {
            steps {
                bat 'node test2.js'
            }
        }
    }
}